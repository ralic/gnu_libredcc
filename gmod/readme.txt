# To use this module do the following:

1. dtoverlay=pwm (and switch off analog audio), this should request gpio 18?

# Todo

- why is the pwm not mapped to memory even though the module is loaded and I call pwm request?
